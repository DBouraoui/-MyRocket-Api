<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Informations de connexion SFTP</title>
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --background-color: #f5f7fa;
            --text-color: #333;
            --border-radius: 6px;
            --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 30px auto;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            overflow: hidden;
        }

        .header {
            background-color: var(--primary-color);
            color: white;
            padding: 20px 30px;
            position: relative;
        }

        .header h1 {
            font-size: 1.8rem;
            font-weight: 600;
        }

        .secure-badge {
            position: absolute;
            top: 20px;
            right: 30px;
            background-color: var(--secondary-color);
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .secure-badge i {
            font-size: 1rem;
        }

        .website-frame {
            padding: 15px 30px;
            background-color: #f8f9fa;
            border-bottom: 1px solid #eaeaea;
        }

        .website-frame a {
            color: var(--secondary-color);
            text-decoration: none;
            font-weight: 500;
        }

        .website-frame a:hover {
            text-decoration: underline;
        }

        .content {
            padding: 30px;
        }

        .card {
            background-color: #f8f9fa;
            border-left: 4px solid var(--secondary-color);
            border-radius: var(--border-radius);
            padding: 20px;
            margin-bottom: 20px;
        }

        .card-title {
            font-size: 1.2rem;
            color: var(--primary-color);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .connection-info {
            list-style-type: none;
        }

        .connection-info li {
            padding: 12px 0;
            border-bottom: 1px dashed #e0e0e0;
            display: grid;
            grid-template-columns: 150px 1fr;
        }

        .connection-info li:last-child {
            border-bottom: none;
        }

        .label {
            font-weight: 600;
            color: var(--primary-color);
        }

        .value {
            font-family: 'Courier New', Courier, monospace;
            background-color: #edf2f7;
            padding: 5px 10px;
            border-radius: 4px;
            position: relative;
        }

        .copy-btn {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            background-color: var(--secondary-color);
            color: white;
            border: none;
            border-radius: 4px;
            padding: 3px 8px;
            font-size: 0.8rem;
            cursor: pointer;
            opacity: 0;
            transition: opacity 0.2s;
        }

        .value:hover .copy-btn {
            opacity: 1;
        }

        .warning {
            margin-top: 30px;
            background-color: rgba(231, 76, 60, 0.1);
            border-left: 4px solid var(--accent-color);
            padding: 15px;
            border-radius: var(--border-radius);
            font-size: 0.9rem;
        }

        .footer {
            padding: 15px 30px;
            background-color: #f8f9fa;
            border-top: 1px solid #eaeaea;
            font-size: 0.9rem;
            color: #666;
            text-align: center;
        }

        @media (max-width: 768px) {
            .container {
                margin: 10px;
            }

            .connection-info li {
                grid-template-columns: 1fr;
                gap: 5px;
            }

            .header h1 {
                font-size: 1.5rem;
            }
        }
    </style>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
    <div class="header">
        <h1>Informations de connexion à votre SFTP</h1>
        <div class="secure-badge">
            <i class="fas fa-lock"></i> Information Sécurisée
        </div>
    </div>

    <div class="website-frame">
        <i class="fas fa-globe"></i> Site web: <a href="{{ urlWebsite }}" target="_blank">{{ urlWebsite }}</a>
    </div>

    <div class="content">
        <div class="card">
            <div class="card-title">
                <i class="fas fa-server"></i> Détails de connexion SFTP
            </div>
            <ul class="connection-info">
                <li>
                    <span class="label">Adresse du serveur</span>
                    <div class="value">
                        {{ configuration.address }}
                        <button class="copy-btn" onclick="copyToClipboard(this)">Copier</button>
                    </div>
                </li>
                <li>
                    <span class="label">Port</span>
                    <div class="value">
                        {{ configuration.port }}
                        <button class="copy-btn" onclick="copyToClipboard(this)">Copier</button>
                    </div>
                </li>
                <li>
                    <span class="label">Nom d'utilisateur</span>
                    <div class="value">
                        {{ configuration.username }}
                        <button class="copy-btn" onclick="copyToClipboard(this)">Copier</button>
                    </div>
                </li>
                <li>
                    <span class="label">Mot de passe</span>
                    <div class="value">
                        {{ configuration.password }}
                        <button class="copy-btn" onclick="copyToClipboard(this)">Copier</button>
                    </div>
                </li>
            </ul>
        </div>

        <div class="warning">
            <i class="fas fa-exclamation-triangle"></i> <strong>Attention:</strong> Ces informations sont confidentielles. Ne les partagez jamais avec des tiers non autorisés et assurez-vous de les conserver dans un endroit sécurisé.
        </div>
    </div>

    <div class="footer">
        <p>Ce document contient des informations confidentielles destinées uniquement à l'usage du destinataire désigné.</p>
    </div>
</div>

<script>
    function copyToClipboard(button) {
        const valueElement = button.parentElement;
        const textToCopy = valueElement.textContent.trim().split('Copier')[0].trim();

        navigator.clipboard.writeText(textToCopy).then(() => {
            const originalText = button.textContent;
            button.textContent = 'Copié!';
            button.style.backgroundColor = '#27ae60';

            setTimeout(() => {
                button.textContent = originalText;
                button.style.backgroundColor = '';
            }, 2000);
        }).catch(err => {
            console.error('Erreur lors de la copie:', err);
        });
    }
</script>
</body>
</html>